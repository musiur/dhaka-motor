const { default: DashboardNav } = require('@/layouts/DashboardNav');

const Profile = () => {
    return (
        <DashboardNav>
            <div>profile</div>
        </DashboardNav>
    );
};

export default Profile;
